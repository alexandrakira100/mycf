<div id="create-message">
  <%= simple_form_for [@fund, @message], remote: true do |f| %>
    <%= f.input :content, label: false %>
    <%= f.submit "send", id:"send-message", class: "btn btn-primary"  %>
  <% end %>
</div>
