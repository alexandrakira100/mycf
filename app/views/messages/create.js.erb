// const btn = document.getElementById('send-message');
// btn.addEventListner("click", (event) => {
// console.log(event);
// });

<% if @message.errors.any? %>
  // handle validation errors later
<% else %>
  var messagesContainer = document.querySelector('.messages-all');
  messagesContainer.insertAdjacentHTML('beforeend', '<%= j render "messages/show", message: @new_message %>')
  var input = document.querySelector('#message_content');
  input.value = '';
  input.blur();
<% end %>

