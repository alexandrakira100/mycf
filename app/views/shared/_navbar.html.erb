<div class="navbar-wagon navbar-fixed-top">

  <div class="col-xs-12 col-sm-4">
    <div class="organise-2">
      <%= link_to root_path, class: "navbar-wagon-brand" do %>
      <% end %>
      <a href="/" class="navbar-wagon-brand">
        <%= image_tag("benchmark-official.jpg") %>
      </a>

    </div>

</div>

  <div class="col-xs-12 col-sm-8">

        <div class="navbar-wagon-right hidden-xs hidden-sm">

          <div style="margin-top: 10px">
            <%= link_to "MY FUNDS", funds_path, class:"navbar-wagon-item navbar-wagon-link" %>
          </div>

          <div style="margin-top: 10px">
           <!--  <%= link_to "ABOUT", about_path, class:"navbar-wagon-item navbar-wagon-link" %> -->
            <a href="#about" class= "navbar-wagon-item navbar-wagon-link">ABOUT</a>
          </div>

          <div style="margin-top: 10px">

            <%= link_to "HELP", help_path, class:"navbar-wagon-item navbar-wagon-link" %>
          </div>
          <% if user_signed_in? %>

             <div class="dropdown">
            <div class="welcome-message", style=" margin-right: 30px; margin-left: 10px;">
              <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><%=current_user.name%>
              <%= image_tag current_user.photo.nil? ? "avatar.png" : current_user.photo, class: "avatar-logo"%></button>
                <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                 <div class="dropdown-item">
                 <%= link_to 'LOGOUT', destroy_user_session_path, method: :delete, class:"login-btn" %>
                </div>
                </div>
                </div>

            </div>
          <% end %>

          <div style=" margin-top: 9px;" >
            <% if user_signed_in? %>
            <% else %>
              <div>
                <%= link_to 'LOGIN', new_user_session_path, class:"rnd-bnt"   %>
              </div>
            <% end %>
          </div>
        </div>

  </div>
</div>
